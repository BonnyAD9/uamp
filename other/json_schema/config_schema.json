{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/BonnyAD9/uamp/master/other/json_schema/config_schema.json",
    "title": "uamp config file",
    "description": "Config file for uamp (audio player)",
    "type": "object",
    "properties": {
        "$schema": {
            "type": "string",
            "description": "The thing that gives you this description"
        },
        "search_paths": {
            "description": "Paths where to search for music.",
            "type": "array",
            "items": {
                "type": "string"
            }
        },
        "recursive_search": {
            "description": "Search songs recursively, including all subdirectories.",
            "type": "boolean",
            "default": true
        },
        "library_path": {
            "description": "Absolute path to the json file with the library. If null, uamp will not save or load the library.",
            "type": ["string", "null"]
        },
        "player_path": {
            "description": "Absolute path to the json file with the playback info. If null, uamp will not save or load the playback.",
            "type": ["string", "null"]
        },
        "audio_extensions": {
            "description": "File extensions that are checked as music.",
            "type": "array",
            "items": {
                "type": "string",
                "pattern": "^[^\\.]*$"
            },
            "default": [
                "flac",
                "mp3",
                "m4a",
                "mp4"
            ]
        },
        "update_library_on_start": {
            "description": "Search for new songs on startup.",
            "type": "boolean",
            "default": true
        },
        "remove_missing_on_load": {
            "description": "Remove songs with invalid paths from the library when loading new songs.",
            "type": "boolean",
            "default": true
        },
        "volume_jump": {
            "description": "Specifies how much the volume should change with heach volume up/down.",
            "type": "number",
            "default": 0.025
        },
        "save_playback_pos": {
            "description": "When playback position is saved, it can be loaded the next time uamp starts. This means that your song will continue playing at the same place that it was when you closed uamp.",
            "type": "string",
            "default": "Never",
            "enum": ["Never", "OnClose", "Always"]
        },
        "save_timeout": {
            "description": "When not null, saves all the data every number of seconds specified here.",
            "type": ["null", "string"],
            "default": "60",
            "pattern": "^([0-9]*d)?([0-9]*:)?([0-9]*:)?([0-9]*|\\.|[0-9]*\\.[0-9]*|\\.[0-9])$"
        },
        "fade_play_pause": {
            "description": "Sets the fade play/pause. Set to 0 to disable fading.",
            "type": "string",
            "default": "0.15",
            "pattern": "^([0-9]*d)?([0-9]*:)?([0-9]*:)?([0-9]*|\\.|[0-9]*\\.[0-9]*|\\.[0-9])$"
        },
        "gapless": {
            "description": "Enable gapless playback.",
            "type": "boolean",
            "default": "true"
        },
        "seek_jump": {
            "description": "Determines how much should be seeked with fast-forward/rewind.",
            "type": "string",
            "default": "10",
            "pattern": "^([0-9]*d)?([0-9]*:)?([0-9]*:)?([0-9]*|\\.|[0-9]*\\.[0-9]*|\\.[0-9])$"
        },
        "server_address": {
            "description": "Address of the server to register and use.",
            "type": "string",
            "default": "127.0.0.1"
        },
        "port": {
            "description": "Port number to use.",
            "type": "integer",
            "default": 8267
        },
        "delete_logs_after": {
            "description": "Logs are deleted after this time.",
            "type": "string",
            "pattern": "^([0-9]*d)?([0-9]*:)?([0-9]*:)?([0-9]*|\\.|[0-9]*\\.[0-9]*|\\.[0-9])$",
            "default": "3d"
        },
        "enable_server": {
            "description": "Enable the server to allow controlling the running instance from the CLI.",
            "type": "boolean",
            "default": true
        },
        "shuffle_current": {
            "description": "Include the currently playing song when shuffling the playlist.",
            "type": "boolean",
            "default": true
        },
        "previous_timeout": {
            "description": "When not null, the action for previous will go to previous only if the last previous action was in the given time, otherwise it will rewind to start.",
            "type": ["string", "null"],
            "pattern": "^([0-9]*d)?([0-9]*:)?([0-9]*:)?([0-9]*|\\.|[0-9]*\\.[0-9]*|\\.[0-9])$",
            "default": null
        },
        "play_on_start": {
            "description": "Start playback automatically when uamp launches.",
            "type": "boolean",
            "default": "false"
        },
        "simple_sorting": {
            "description": "Sort library songs only by the selected property, ignoring secondary properties.",
            "type": "boolean",
            "default": false
        },
        "control_aliases": {
            "description": "Aliases for actions.",
            "type": "object",
            "additionalProperties": {
                "description": "Defines what this alias does. Aliases can also have arguments, in that case use '\"alias-name\": \"[arg1, arg2]something:${arg1} ${arg2}'",
                "type": "string"
            },
            "default": {
                "repeat": "pj pp=play spea=repeat",
                "repeat-once": "pj pp=play spea",
                "endless-mix": "sp=@rng pj pp=play pap=m spea=endless-mix",
                "pcont": "pop pp=play",
                "palb": "{name}:push=a:${name}@+a pp=play spea=pcont"
            }
        },
        "default_playlist_end_action": {
            "description": "This will be used as playlist end action if the end action is not set.",
            "type": ["string", null],
            "default": null
        },
        "client_image_lookup": {
            "description": "Clients will look up song images.",
            "type": "boolean",
            "default": true
        },
        "update_mode": {
            "description": "Determines to which will uamp update.",
            "type": ["string", "object"],
            "pattern": "LatestTag|LatestCommit",
            "properties": {
                "Branch": {
                    "description": "Select branch from which uamp will update.",
                    "type": "string"
                }
            },
            "default": "LatestTag"
        },
        "update_remote": {
            "description": "Determines remote git repository from which uamp will update.",
            "type": "string",
            "default": "https://github.com/BonnyAD9/uamp.git"
        },
        "system_player": {
          "description": "Integrate with the system (Linux only, via MPRIS).",
          "type": "boolean",
          "default": true
        },
        "skin": {
          "description": "Path to skin to use. Should be absolute.",
          "type": "string"
        },
        "auto_restart": {
            "description": "Automatically restart running instance if it detects changed binary.",
            "type": "boolean",
            "default": true
        },
        "cache_path": {
            "description": "Path to folder containing precomputed data from uamp.",
            "type": "string"
        },
        "default_run_type": {
          "description": "The default way that uamp will start up when without arguments.",
          "type": "string",
          "pattern": "Background|WebClient",
          "default": "WebClient"
        },
        "web_client_command": {
          "description": "The command used to open web client. If null, use the default browser. `${ADDRESS}` will be replaced with the target address to the web client.",
          "type": "string",
          "default": null
        }
    }
}
